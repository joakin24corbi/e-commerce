import { render } from "@testing-library/react"

import SplashScreen from "views/splash-screen/SplashScreen"

describe('Splash screen', () => {
    it('should be rendered with image and loader', () => {
        render(<SplashScreen />);

        const image = screen.getBy
        expect()
    })
})